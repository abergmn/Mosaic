"""
is_active.py

Helper for checking if a host is active/reachable.
"""

def is_host_active(ip: str) -> bool:
    """
    Determine if a host is active by probing it.

    Args:
        ip (str): The IP address of the host to check.

    Returns:
        bool: True if the host is active/reachable, False otherwise.
    """

    return
